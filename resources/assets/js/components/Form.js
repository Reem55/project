import React, { Component } from 'react';

const Form = ({form}) => {
    if(!form) {
        return (
            <div>Form doesn't exist</div>
        );
    }

    return(
        <div className="card">
            <div className="card-body">
                
            </div>
        </div>
    )
}